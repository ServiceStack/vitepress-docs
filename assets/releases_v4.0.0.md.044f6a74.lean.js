import{_ as e,c as t,o as a,a as n}from"./app.14440598.js";const m=`{"title":"v4 Release Notes","description":"","frontmatter":{"title":"v4 Release Notes","slug":"v4-0-0"},"headers":[{"level":2,"title":"The big refactor of v4","slug":"the-big-refactor-of-v4"},{"level":3,"title":"RequestContext now merged into new IRequest / IResponse classes:","slug":"requestcontext-now-merged-into-new-irequest-iresponse-classes"},{"level":3,"title":"Community v4 migration notes","slug":"community-v4-migration-notes"},{"level":2,"title":"Server-side Async Support","slug":"server-side-async-support"},{"level":2,"title":"ServiceStack Client Task-based Async","slug":"servicestack-client-task-based-async"},{"level":3,"title":"Custom Silverlight and Android builds","slug":"custom-silverlight-and-android-builds"},{"level":2,"title":"Signed NuGet Packages","slug":"signed-nuget-packages"},{"level":3,"title":"ServiceStack.Interfaces is now strong-named","slug":"servicestack-interfaces-is-now-strong-named"},{"level":3,"title":"Add Code-first Attributes at runtime, de-coupled from POCO's","slug":"add-code-first-attributes-at-runtime-de-coupled-from-poco-s"},{"level":3,"title":"The Virtual FileSystem","slug":"the-virtual-filesystem"},{"level":2,"title":"API-first development","slug":"api-first-development"},{"level":2,"title":"Improved Razor Support","slug":"improved-razor-support"},{"level":3,"title":"CORS Feature","slug":"cors-feature"},{"level":2,"title":"Authentication","slug":"authentication"},{"level":3,"title":"AppSettings","slug":"appsettings"},{"level":3,"title":"Nested Request DTOs","slug":"nested-request-dtos"},{"level":3,"title":"Localized symbols","slug":"localized-symbols"},{"level":2,"title":"Other New Web Framework Features","slug":"other-new-web-framework-features"},{"level":2,"title":"OrmLite","slug":"ormlite"},{"level":3,"title":"Improved Consistency","slug":"improved-consistency"},{"level":3,"title":"OrmLite extension methods are now mockable","slug":"ormlite-extension-methods-are-now-mockable"},{"level":3,"title":"Support for references, POCO style","slug":"support-for-references-poco-style"},{"level":2,"title":"ServiceStack.Text","slug":"servicestack-text"},{"level":3,"title":"HTTP Utils are now mockable","slug":"http-utils-are-now-mockable"},{"level":2,"title":"New pre-release MyGet Feeds","slug":"new-pre-release-myget-feeds"}],"relativePath":"releases/v4.0.0.md","lastUpdated":1634495308430}`,s={},o=n(`__VP_STATIC_START__<p>We&#39;re happy to announce that after months of intense development, v4-beta of ServiceStack has finally been released to NuGet!</p><p>As <a href="https://plus.google.com/+DemisBellot/posts/g8TcZaE7bv9" target="_blank" rel="noopener noreferrer">announced in August</a> to ensure it&#39;s continued development, ServiceStack has moved to a self-sustaining commercial model for commercial usage of ServiceStack from <strong>v4+ onwards</strong>. It&#39;s the first time we&#39;ve been able to commit full-time resources to the project and is what has ensured continued investment and enabled v4 to be possible, with even more exciting features in the pipeline and roadmap for 2014.</p><h4 id="free-usage-for-small-and-oss-projects" tabindex="-1">Free Usage for Small and OSS Projects <a class="header-anchor" href="#free-usage-for-small-and-oss-projects" aria-hidden="true">#</a></h4><p>We&#39;re also happy to announce that v4 includes <a href="https://servicestack.net/download#free-quotas" target="_blank" rel="noopener noreferrer">free quotas</a> allowing the free usage of all of ServiceStack for small projects and evaluation purposes. Whilst OSS projects are able to use the source code on GitHub under the <a href="https://github.com/ServiceStack/ServiceStack/blob/master/license.txt" target="_blank" rel="noopener noreferrer">AGPL/FOSS License Exception</a>, and the older <a href="https://github.com/ServiceStackV3/ServiceStackV3" target="_blank" rel="noopener noreferrer">v3 of ServiceStack</a> continues to be available under the <a href="https://github.com/ServiceStack/ServiceStack/blob/v3/LICENSE" target="_blank" rel="noopener noreferrer">BSD license</a>.</p><h4 id="upgrading-from-v3" tabindex="-1">Upgrading from v3 <a class="header-anchor" href="#upgrading-from-v3" aria-hidden="true">#</a></h4><p>Whilst we recommend starting with <strong>v4</strong> for greenfield projects, v4 has seen significant changes since v3 that will require some development effort to upgrade. During the upgrade we recommend using a tool like <a href="http://www.jetbrains.com/resharper/" target="_blank" rel="noopener noreferrer">ReSharper</a> to be able to easily find and update reference of any types that have moved.</p><h1 id="what-s-new-in-v4" tabindex="-1">What&#39;s new in v4 <a class="header-anchor" href="#what-s-new-in-v4" aria-hidden="true">#</a></h1><p>The major version upgrade of ServiceStack to v4 has provided us a long sought <strong>breaking window</strong> opportunity allowing us to re-factor, simplify, clean-up and fix all the warts and cruft that has been lingering in the ServiceStack code-base since its beginning - that due to backwards compatibility we were reluctant to remove. Whilst v4 has seen significant changes to the code-base, all existing tests are passing again with additional tests added for new functionality.</p><p>We managed to retain a lot of the user-facing API&#39;s (E.g New API, AppHost, Config) which were already considered ideal so ideally upgrading shouldn&#39;t be too disruptive in the normal use-cases.</p><p>v4 provides us a great foundation to build on that will be further improved during the beta by focusing on stability and fixing any reported issues as well as updating existing documentation to match v4&#39;s implementation and publish new examples showcasing v4&#39;s new features.</p><h2 id="the-big-refactor-of-v4" tabindex="-1">The big refactor of v4 <a class="header-anchor" href="#the-big-refactor-of-v4" aria-hidden="true">#</a></h2><p>This was the biggest re-factor in ServiceStack&#39;s history which at the end resulted in a much leaner, simplified, consistent and internal logically-structured code-base that&#39;s much easier to reason about, where even before adding any features the main ServiceStack repository saw:</p><pre><code>1,192 changed files with 18,325 additions and 29,505 deletions. 
</code></pre><p>The number of deletions is indicative of how much legacy code was able to be removed, with much of the internals having been heavily restructured. Some of the highlights during the re-factor include:</p><ul><li>All projects have been upgraded to .NET 4.0, except ServiceStack.Razor which is .NET 4.5 to use the latest version of Razor</li><li>All obsolete/unused/shims/duplicate functionality and built-up cruft has now been removed (inc. the Old Api).</li><li>State and configuration are now cohesively organized where now all AppHost&#39;s share the same <code>ServiceStackHost</code> base class which now maintains all state in ServiceStack, inc. the empty <code>BasicAppHost</code> that&#39;s used for unit testing which now shares much of the same state/context as Integration tests.</li><li>Many namespaces and some concepts have been collapsed (e.g &#39;Endpoint&#39;), resulting in ServiceStack projects requiring fewer namespaces</li><li>All DTO&#39;s and extension methods and common user-facing classes have been moved to the base <code>ServiceStack</code> namespace - allowing them to be much easier to find.</li><li>Re-organization of projects, <strong>NuGet packages now map 1:1 with ServiceStack projects</strong> for finer-grained control of dependencies: <ul><li><strong>ServiceStack.Interfaces</strong> NuGet project created and ServiceInterface has been merged into <strong>ServiceStack</strong></li><li><strong>ServiceStack</strong> NuGet package now only depends <strong>ServiceStack.Common</strong> and <strong>ServiceStack.Client</strong></li><li>A new <strong>ServiceStack.Server</strong> project exists for functionality requiring dependencies on OrmLite or Redis, inc. RedisMqServer and OrmLiteAuthRepository.</li><li><strong>ServiceStack.Client</strong> contains all the HTTP, SOAP and MQ Service Clients that have been split from <strong>ServiceStack.Common</strong> and only depends on ServiceStack.Interfaces and ServiceStack.Text (making it easier to maintain custom builds in future).</li></ul></li><li>EndpointHostConfig is now <code>HostConfig</code> and is limited to just Configuration, e.g. handlers like <code>CustomErrorHttpHandlers</code>, <code>RawHttpHandlers</code>, <code>GlobalHtmlErrorHttpHandler</code> have been moved to ServiceStackHost.</li><li>EndpointHost is gone and replaced by the static <code>HostContext</code> class which doesn&#39;t contain any state itself, it&#39;s just a static convenience wrapper around <code>ServiceStackHost.Instance</code> (where all state is maintained).</li><li>Removed all &#39;where T:&#39; constraints where possible</li><li>Removed <code>ConfigurationResourceManager</code>, use <code>AppSettings</code> instead</li><li>The <code>ServiceStack.WebHost.Endpoints.ServiceStackHttpHandlerFactory</code> used in Web.config&#39;s handler mapping has been renamed to just <code>ServiceStack.HttpHandlerFactory</code></li><li><code>Config.ServiceStackHandlerFactoryPath</code> has been renamed to <code>Config.HandlerFactoryPath</code>.</li><li>Predefined routes have been renamed from <code>/syncreply</code>, <code>/asynconeway</code> to just <code>/reply</code>, <code>/oneway</code></li><li>ServiceManager has been merged into <code>ServiceController</code>.</li><li>The <strong>ServiceStack.Logging</strong> and <strong>ServiceStack.Contrib</strong> v4 projects have been merged into the major ServiceStack repo.</li><li>The dynamic session <code>base.Session</code> has been renamed to <code>base.SessionBag</code> to better reflect its semantics.</li><li>The <a href="/auto-mapping.html">Auto Mapping</a> Utils extension methods were renamed from <code>TFrom.TranslateTo&lt;T&gt;()</code> to <code>TFrom.ConvertTo&lt;T&gt;()</code>.</li><li>The <code>RequestFilters</code> and <code>ResponseFilters</code> were renamed to <code>GlobalRequestFilters</code> and <code>GlobalResponseFilters</code> which matches naming in the client <code>ServiceClientBase.GlobalRequestFilter</code>.</li><li>New <code>GlobalMessageRequestFilters</code> and <code>GlobalMessageResponseFilters</code> have been added which are instead used by non-HTTP endpoints use, e.g. MQ.</li><li><code>CustomHttpHandlers</code> has been renamed to <code>CustomErrorHttpHandlers</code></li><li>The <strong>LocalHttpWebRequestFilter</strong> and <strong>LocalHttpWebResponseFilter</strong> in the Service Clients were renamed to just <code>RequestFilter</code> and <code>ResponseFilter</code></li><li>The Global <strong>HttpWebRequestFilter</strong> and <strong>HttpWebResponseFilter</strong> filters were also renamed to <code>GlobalRequestFilter</code> and <code>GlobalResponseFilter</code> respectively.</li></ul><h3 id="requestcontext-now-merged-into-new-irequest-iresponse-classes" tabindex="-1">RequestContext now merged into new IRequest / IResponse classes: <a class="header-anchor" href="#requestcontext-now-merged-into-new-irequest-iresponse-classes" aria-hidden="true">#</a></h3><p>An annoyance remaining in the ServiceStack code-base was RequestContext and its relationship with its IHttpRequest and IHttpResponse classes. This was originally modeled after <a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a>&#39;s relationship with HttpContext and its child HttpRequest/HttpResponse classes. Pragmatically speaking this model isn&#39;t ideal, as there was functionality spread across all 3 classes, many times duplicated. It was also not obvious how to retrieve IHttpRequest/IHttpResponse classes from a RequestContext and creating a RequestContext from outside of ServiceStack required more knowledge and effort than it should have.</p><p>The new model adopts a flattened structure similar to Dart&#39;s server HttpRequest (<a href="http://bit.ly/19WUxLJ" target="_blank" rel="noopener noreferrer">http://bit.ly/19WUxLJ</a>) which sees the <code>IRequestContext</code> eliminated in favour of a single <code>IRequest</code> class that also makes available direct access to the Response.</p><p>This now becomes much easier to create a Request from outside of ServiceStack with an <a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a> or HttpListener HttpContext e.g:</p><div class="language-csharp"><pre><code><span class="token class-name"><span class="token keyword">var</span></span> service <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyService</span> <span class="token punctuation">{</span>
    Request <span class="token operator">=</span> HttpContext<span class="token punctuation">.</span>Current<span class="token punctuation">.</span><span class="token function">ToRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>

<span class="token class-name"><span class="token keyword">var</span></span> service <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyService</span> <span class="token punctuation">{</span>
    Request <span class="token operator">=</span> httpListenerContext<span class="token punctuation">.</span><span class="token function">ToRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre></div><p>There&#39;s also direct access to the Response from a Request with:</p><div class="language-csharp"><pre><code><span class="token class-name">IResponse</span> response <span class="token operator">=</span> Request<span class="token punctuation">.</span>Response<span class="token punctuation">;</span>
</code></pre></div><h4 id="asp-net-wrappers-now-only-depends-on-httpcontextbase" tabindex="-1"><a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a> wrappers now only depends on HttpContextBase <a class="header-anchor" href="#asp-net-wrappers-now-only-depends-on-httpcontextbase" aria-hidden="true">#</a></h4><p>Also the <a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a> <code>IHttpRequest</code> wrappers bind to the newer and mockable HttpContextBase / HttpRequestBase / HttpResponseBase classes which now makes it easier to call services from newer web frameworks like MVC with:</p><div class="language-csharp"><pre><code><span class="token class-name"><span class="token keyword">var</span></span> service <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">MyService</span> <span class="token punctuation">{</span>
    Request <span class="token operator">=</span> <span class="token keyword">base</span><span class="token punctuation">.</span>HttpContext<span class="token punctuation">.</span><span class="token function">ToRequest</span><span class="token punctuation">(</span><span class="token punctuation">)</span>
<span class="token punctuation">}</span>
</code></pre></div><p>The biggest user-facing change was renaming the IHttpRequest/IHttpResponse classes to IRequest/IResponse which is more indicative to what they represent, i.e. the Request and Response classes for all endpoints including MQ and future TCP endpoints. Now only HTTP Requests implement IHttpRequest/IHttpResponse which lets you add logic targeting only HTTP Services with a simple type check:</p><div class="language-csharp"><pre><code><span class="token class-name"><span class="token keyword">var</span></span> httpReq <span class="token operator">=</span> request <span class="token keyword">as</span> <span class="token class-name">IHttpRequest</span><span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>httpReq <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">//Add logic for HTTP Requests...</span>
<span class="token punctuation">}</span>
</code></pre></div><p>Accessing the IHttpResponse works the same way, e.g:</p><div class="language-csharp"><pre><code><span class="token class-name"><span class="token keyword">var</span></span> httpRes <span class="token operator">=</span> Request<span class="token punctuation">.</span>Response <span class="token keyword">as</span> <span class="token class-name">IHttpResponse</span><span class="token punctuation">;</span>
<span class="token keyword">if</span> <span class="token punctuation">(</span>httpRes <span class="token operator">!=</span> <span class="token keyword">null</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
    <span class="token comment">//...</span>
<span class="token punctuation">}</span>
</code></pre></div><p>We&#39;re still going to add extension methods on IRequest/IResponse to make it easier to discover new functionality, but for HTTP functionality on non-HTTP requests these would just be a NO-OP rather than throw an exception.</p><h3 id="community-v4-migration-notes" tabindex="-1">Community v4 migration notes <a class="header-anchor" href="#community-v4-migration-notes" aria-hidden="true">#</a></h3><ul><li><a href="http://camtucker.blogspot.ca/2014/01/updating-to-servicestack-v40.html?view=classic" target="_blank" rel="noopener noreferrer">Upgrading OrmLite and ServiceStack to v4</a> by <a href="http://camtucker.blogspot.ca/" target="_blank" rel="noopener noreferrer">@camtucker</a></li></ul><hr><h1 id="new-features-in-v4" tabindex="-1">New Features in v4 <a class="header-anchor" href="#new-features-in-v4" aria-hidden="true">#</a></h1><h2 id="server-side-async-support" tabindex="-1">Server-side Async Support <a class="header-anchor" href="#server-side-async-support" aria-hidden="true">#</a></h2><p>The <a href="http://bit.ly/16qCiy1" target="_blank" rel="noopener noreferrer">most requested feature</a>, Server-side async support has now been implemented! This was surprisingly easy to do where now all HttpHandlers in ServiceStack inherit from a common <code>HttpAsyncTaskHandler</code> base class that now implements <code>IHttpAsyncHandler</code>. This lets you return an async Task from your Service in any number of ways as shown in <a href="http://bit.ly/1cOJ3hR" target="_blank" rel="noopener noreferrer">http://bit.ly/1cOJ3hR</a></p><p>E.g. Services can now have either an object, Task or async Task return types that can return a started or non-started task (which we&#39;ll start ourselves). This transition went as smooth as it could where all existing services continuing to work as before and all tests passing.</p><h2 id="servicestack-client-task-based-async" tabindex="-1"><a href="/csharp-client.html">ServiceStack Client</a> Task-based Async <a class="header-anchor" href="#servicestack-client-task-based-async" aria-hidden="true">#</a></h2><p>In matching the new server-side async story and now that all projects have been upgraded to .NET 4.0, all Service Clients have been changed to return .NET 4.0 Task&#39;s for all async operations so they can be used in C#&#39;s async/await methods. Some examples of Async in action: <a href="http://bit.ly/17ps94C" target="_blank" rel="noopener noreferrer">http://bit.ly/17ps94C</a></p><p>The Async API&#39;s also provide a <strong>OnDownloadProgress</strong> callback which you can tap into to provide a progress indicator in your UI, E.g: <a href="http://bit.ly/19ALXUW" target="_blank" rel="noopener noreferrer">http://bit.ly/19ALXUW</a></p><h4 id="use-any-request-dto-in-client-api-s" tabindex="-1">Use any Request DTO in Client API&#39;s <a class="header-anchor" href="#use-any-request-dto-in-client-api-s" aria-hidden="true">#</a></h4><p>ServiceClient API&#39;s that used to only accept Request DTO&#39;s with a <code>IReturn</code> marker, now have <code>object</code> overloads so they can be used for unmarked Request DTO&#39;s as well.</p><h3 id="custom-silverlight-and-android-builds" tabindex="-1">Custom Silverlight and Android builds <a class="header-anchor" href="#custom-silverlight-and-android-builds" aria-hidden="true">#</a></h3><p>We&#39;ve added custom <strong>Silverlight</strong> and <strong>Android</strong> automated builds for ServiceStack.Client allowing the client libraries to be available in even more environments - with more to follow.</p><h2 id="signed-nuget-packages" tabindex="-1">Signed NuGet Packages <a class="header-anchor" href="#signed-nuget-packages" aria-hidden="true">#</a></h2><p>The following Signed NuGet packages are available for core ServiceStack projects in separate NuGet packages using the .Signed suffix:</p><ul><li>ServiceStack.Client.Signed</li><li>ServiceStack.Text.Signed</li><li>ServiceStack.Redis.Signed</li><li>ServiceStack.OrmLite.Signed</li><li>ServiceStack.OrmLite.SqlServer.Signed</li><li>ServiceStack.ServiceStack.Signed</li><li>ServiceStack.ServiceStack.Razor.Signed</li><li>ServiceStack.ServiceStack.Server.Signed</li><li>ServiceStack.Common.Signed</li></ul><h3 id="servicestack-interfaces-is-now-strong-named" tabindex="-1">ServiceStack.Interfaces is now strong-named <a class="header-anchor" href="#servicestack-interfaces-is-now-strong-named" aria-hidden="true">#</a></h3><p>In order to be able to have signed clients sharing types with non-signed ServiceStack instances, the DTO models and ServiceStack.Interfaces need to be signed. It was added in the most defensive way possible where <strong>ServiceStack.Interfaces.dll</strong> is the only dll that&#39;s strong-named by default. This should cause minimal friction as it is an impl-free assembly that rarely sees any changes. We&#39;re also keeping the AssemblyVersion which makes up the strong-name at a constant <code>4.0</code> whilst the benign AssemblyFileVersion will report the true version number.</p><h3 id="add-code-first-attributes-at-runtime-de-coupled-from-poco-s" tabindex="-1">Add Code-first Attributes at runtime, de-coupled from POCO&#39;s <a class="header-anchor" href="#add-code-first-attributes-at-runtime-de-coupled-from-poco-s" aria-hidden="true">#</a></h3><p>Inspection of all Metadata attributes in ServiceStack now uses ServiceStack.Text&#39;s attribute reflection API&#39;s which support adding of type and property metadata attributes dynamically. This now lets you add the same behavior normally only available via attributes, dynamically at StartUp. Some benefits of this include: being able to keep <a href="http://bit.ly/1e5IQqS" target="_blank" rel="noopener noreferrer">unattributed data model POCOs in OrmLite</a> or to <a href="https://github.com/ServiceStack/ServiceStack/blob/d93ad805c8c8ffce8e32365e4217c65c19069cf0/tests/ServiceStack.WebHost.Endpoints.Tests/RuntimeAttributeTests.cs" target="_blank" rel="noopener noreferrer">extend built-in and external Request DTOs and Services</a> with enhanced functionality that was previously only available using attributes.</p><h4 id="fluent-route-configuration-available-in-reverse-routing" tabindex="-1">Fluent route configuration available in <a href="/routing.html#reverse-routing">Reverse Routing</a> <a class="header-anchor" href="#fluent-route-configuration-available-in-reverse-routing" aria-hidden="true">#</a></h4><p>Leveraging the dynamic attribute support, we now include fluent Route definitions when retrieving relative or absolute urls in <a href="/routing.html#reverse-routing">Reverse Routing</a>, which can be used in Services when returning urls in responses and is also used in Service Clients to determine which routes to use. Note: as Fluent Routes are defined in the AppHost, they aren&#39;t registered and therefore not available in disconnected .NET client applications - so using <code>[Route]</code> attributes on Request DTO&#39;s remains the best way to share route definitions on both client and server.</p><p>Priority was added to <code>[Route]</code> attributes so auto-generated routes are given less precedence than explicit user-defined custom routes when selecting the best matching route to use.</p><h3 id="the-virtual-filesystem" tabindex="-1">The Virtual FileSystem <a class="header-anchor" href="#the-virtual-filesystem" aria-hidden="true">#</a></h3><p>The Virtual FileSystem is now fully integrated into the rest of ServiceStack, this enables a few interesting things:</p><ul><li>The <code>Config.WebHostPhysicalPath</code> sets where you want physical files in ServiceStack to be served from</li><li>You can now access static files when ServiceStack is mounted at a custom path, e.g. /api/default.html will serve the static file at ~/default.html</li><li>By Default, ServiceStack falls back (i.e when no physical file exists) to looking for Embedded Resource Files inside dlls.</li><li>You can specify the number and precedence of which Assemblies it looks at with <code>Config.EmbeddedResourceSources</code> which by default looks at: <ul><li>The assembly that contains your AppHost</li><li><strong>ServiceStack.dll</strong></li></ul></li></ul><p>The VFS now elegantly lets you replace built-in ServiceStack templates with your own by simply copying the metadata or <a href="http://bit.ly/164YbrQ" target="_blank" rel="noopener noreferrer">HtmlFormat Template files</a> you want to customize and placing them in your folder at:</p><pre><code>/Templates/HtmlFormat.html        // The auto HtmlFormat template
/Templates/IndexOperations.html   // The /metadata template
/Templates/OperationControl.html  // Individual operation template
</code></pre><p>This works because the ServiceStack.dll is the last assembly in <code>Config.EmbeddedResourceSources</code>.</p><h2 id="api-first-development" tabindex="-1">API-first development <a class="header-anchor" href="#api-first-development" aria-hidden="true">#</a></h2><p>We&#39;re starting to optimize ServiceStack&#39;s HTML story around an <strong>API-first</strong> style of web development (particularly well suited to ServiceStack) in which services are developed so they naturally support both web and native clients from the start. Effectively this means that the HTML views are just another client that escapes C# earlier and leverages JS+Ajax to provide its dynamic functionality, and any HTML-specific functionality is encouraged to be kept in Razor views rather than using post backs to generate different server-side HTML representations.</p><p>Having developed the new <a href="https://servicestack.net" target="_blank" rel="noopener noreferrer">servicestack.net website</a> in this way, we&#39;ve found it to be a lot more productive and responsive than standard server-side MVC development that we we&#39;re accustomed to in .NET as JavaScript ends up being more mallable and flexible language with a smaller and reflective surface type area making it better suited in string manipulation, generating HTML views, consuming ajax services, event handling, DOM binding and manipulation, etc.</p><p>We&#39;ve begun taking advantage of the Virtual FileSystem to ship embedded resources enhancing ServiceStack&#39;s JS integration with client-side libraries like <a href="https://github.com/ServiceStack/ServiceStack/blob/master/src/ServiceStack/js/ss-utils.js" target="_blank" rel="noopener noreferrer">ss-utils.js</a> that we maintain and update alongside the rest of ServiceStack&#39;s dlls. Whilst we intend to create more examples in the near future showcasing this functionality, here&#39;s an overview of what&#39;s been added:</p><ul><li>ss-utils.js available in your local ServiceStack webhost at <code>/js/ss-utils.js</code></li><li>Inspired by AngularJS we&#39;ve added <strong>declarative</strong> support over jQuery, letting you declaratively register and trigger events, bind values to HTML elements, register document handlers, etc, saving a lot of boilerplate than normal jQuery (more on this soon)</li><li>Enhanced HTML forms with integration with ServiceStack validation, adds responsive UX/behavior, follows soft redirects</li><li>Server-side responses can be decorated with Soft redirects with <code>HttpResult.SoftRedirect</code> or client events with <code>HttpResult.TriggerEvent</code></li><li>Use <code>(&quot;a&quot;).setActiveLinks()</code> to automatically set the active link and containing menu items for the current page</li><li>Use <code>$(&quot;input&quot;).change($.ss.clearAdjacentError)</code> to clear highlighted errors as users correct their inputs</li><li>Use <code>T.AsRawJson()</code> extension method to serialize C# models into JSON literals that are natively accessible in JS</li><li>Use <code>T.ToGetUrl()</code>, <code>T.ToPostUrl()</code> to resolve service urls from typed Request DTOs (no code-gen required)</li></ul><h2 id="improved-razor-support" tabindex="-1">Improved Razor Support <a class="header-anchor" href="#improved-razor-support" aria-hidden="true">#</a></h2><h4 id="improved-server-side-validation" tabindex="-1">Improved Server-side validation <a class="header-anchor" href="#improved-server-side-validation" aria-hidden="true">#</a></h4><p>The server-side validation story has also been improved with MVC&#39;s HTML INPUT and Validation Helpers rewritten to look at ServiceStack error responses (earlier lost in the upgrade to Razor 2.0) and making use of the same bootstrap conventional classes that the client-side Ajax validation uses letting you maintain a single style of error feedback for both validation styles. It also now looks at state contained in the POST&#39;ed data when rendering the HTML INPUT controls.</p><h4 id="fallback-routes" tabindex="-1">Fallback Routes <a class="header-anchor" href="#fallback-routes" aria-hidden="true">#</a></h4><p>The default Razor views are now also processed by the <code>FallbackRoute</code> if one exists, enhancing the story for Single Page Apps who want requests to un-specified routes to be handled by client-side routing instead of returning 404&#39;s.</p><h4 id="pre-request-filters" tabindex="-1">Pre-Request filters <a class="header-anchor" href="#pre-request-filters" aria-hidden="true">#</a></h4><p>Direct (i.e. No Controller) Razor views and static file handlers now have pre-request filters applied to them, so they can be used for adding global behavior across all ServiceStack service and page requests.</p><h4 id="precompilation-option-for-razor-views" tabindex="-1">Precompilation option for Razor Views <a class="header-anchor" href="#precompilation-option-for-razor-views" aria-hidden="true">#</a></h4><p>New options have been added to RazorFormat <code>PrecompilePages</code> and <code>WaitForPrecompilationOnStartup</code> that allow you to precompile razor views on startup and specify whether or not you want to wait for compilation to complete are now options available when registering the <code>RazorFormat</code>. As these can slow down dev iteration times they are not done when <code>Config.DebugMode</code> (aka development mode), but are otherwise enabled by default for production.</p><h4 id="other-razor-improvements" tabindex="-1">Other Razor Improvements <a class="header-anchor" href="#other-razor-improvements" aria-hidden="true">#</a></h4><ul><li>More functionality was added to Razor Views matching the same API&#39;s available in ServiceStack&#39;s base <code>Service</code> class</li><li>RenderSection/IsSectionDefined now looks in all connected views.</li><li><code>GetAbsoluteUrl</code>, <code>IsPostBack</code>, <code>GetErrorStatus()</code>, <code>GetErrorMessage()</code> convience methods added</li></ul><h3 id="cors-feature" tabindex="-1">CORS Feature <a class="header-anchor" href="#cors-feature" aria-hidden="true">#</a></h3><p>CorsFeature now by default automatically handles all HTTP <code>OPTIONS</code> requests so you no longer have to explicitly allow for OPTION requests in your routes: <a href="http://bit.ly/19HbMVf" target="_blank" rel="noopener noreferrer">http://bit.ly/19HbMVf</a></p><p>Can be disabled with:</p><div class="language-csharp"><pre><code>Plugins<span class="token punctuation">.</span><span class="token function">Add</span><span class="token punctuation">(</span><span class="token keyword">new</span> <span class="token constructor-invocation class-name">CorsFeature</span> <span class="token punctuation">{</span> AutoHandleOptionsRequests <span class="token operator">=</span> <span class="token boolean">false</span> <span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre></div><h2 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-hidden="true">#</a></h2><p>The Auth Tables are now called <strong>UserAuth</strong> and <strong>UserAuthDetails</strong> and implements the IUserAuth and IUserAuthDetails interfaces. For advanced customization, these tables can now be extended using custom models inheriting these interfaces by using the generic AuthRepository types, e.g:</p><ul><li><code>OrmLiteAuthRepository&lt;TUserAuth, TUserAuthDetails&gt;</code></li><li><code>RedisAuthRepository&lt;TUserAuth, TUserAuthDetails&gt;</code></li></ul><p>Where the common non-generic <strong>OrmLiteAuthRepository</strong> is just a concrete impl inheriting from <code>OrmLiteAuthRepository&lt;UserAuth, UserAuthDetails&gt;</code>. Use <code>InitSchema()</code> to ensure missing Auth Tables are created at registration.</p><h4 id="new-optional-userauthrole-table-added" tabindex="-1">New optional UserAuthRole table added <a class="header-anchor" href="#new-optional-userauthrole-table-added" aria-hidden="true">#</a></h4><p>A new <code>UserAuthRole</code> class was created for users who would prefer roles to be managed in separate tables rather than blobbed with the UserAuth table and session. E.g. You can change your custom session to check the database for asserting required users and permissions with:</p><div class="language-csharp"><pre><code><span class="token keyword">public</span> <span class="token keyword">class</span> <span class="token class-name">CustomUserSession</span> <span class="token punctuation">:</span> <span class="token type-list"><span class="token class-name">AuthUserSession</span></span>
<span class="token punctuation">{</span>
    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">HasRole</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">string</span></span> role<span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
        <span class="token keyword">using</span> <span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">var</span></span> db <span class="token operator">=</span> HostContext<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">TryResolve</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IDbConnectionFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
        <span class="token punctuation">{</span>
            <span class="token keyword">return</span> db<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Count</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>UserAuthRole<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> 
                q<span class="token punctuation">.</span>UserAuthId <span class="token operator">==</span> <span class="token keyword">int</span><span class="token punctuation">.</span><span class="token function">Parse</span><span class="token punctuation">(</span>UserAuthId<span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> q<span class="token punctuation">.</span>Role <span class="token operator">==</span> role<span class="token punctuation">)</span> <span class="token operator">&gt;</span> <span class="token number">0</span><span class="token punctuation">;</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>

    <span class="token keyword">public</span> <span class="token keyword">override</span> <span class="token return-type class-name"><span class="token keyword">bool</span></span> <span class="token function">HasPermission</span><span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">string</span></span> permission<span class="token punctuation">)</span>
    <span class="token punctuation">{</span>
        <span class="token keyword">using</span> <span class="token punctuation">(</span><span class="token class-name"><span class="token keyword">var</span></span> db <span class="token operator">=</span> HostContext<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">TryResolve</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IDbConnectionFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">Open</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span>
        <span class="token punctuation">{</span>
            <span class="token keyword">return</span> db<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Count</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>UserAuthRole<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>q <span class="token operator">=&gt;</span> 
                q<span class="token punctuation">.</span>UserAuthId <span class="token operator">==</span> <span class="token keyword">int</span><span class="token punctuation">.</span><span class="token function">Parse</span><span class="token punctuation">(</span>UserAuthId<span class="token punctuation">)</span> <span class="token operator">&amp;&amp;</span> q<span class="token punctuation">.</span>Permission <span class="token operator">==</span> permission<span class="token punctuation">)</span> <span class="token operator">&gt;</span> <span class="token number">0</span><span class="token punctuation">;</span>
        <span class="token punctuation">}</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span>
</code></pre></div><h4 id="support-for-max-login-attempts" tabindex="-1">Support for Max Login Attempts <a class="header-anchor" href="#support-for-max-login-attempts" aria-hidden="true">#</a></h4><p>The <code>OrmLiteAuthRepository</code> now supports automatically locking out user accounts after reaching the maximum number of Login attempts which can be specified at registration, e.g:</p><div class="language-csharp"><pre><code>container<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Register</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IAuthRepository<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span>c <span class="token operator">=&gt;</span>
    <span class="token keyword">new</span> <span class="token constructor-invocation class-name">OrmLiteAuthRepository</span><span class="token punctuation">(</span>c<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Resolve</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IDbConnectionFactory<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span> <span class="token punctuation">{</span>
        MaxLoginAttempts <span class="token operator">=</span> appSettings<span class="token punctuation">.</span><span class="token function">Get</span><span class="token punctuation">(</span><span class="token string">&quot;MaxLoginAttempts&quot;</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">)</span>
    <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre></div><p>To opt-in to use the new locking behavior provide a value for <code>MaxLoginAttempts</code> as shown above. The above registration first uses the value overridable in appSettings if it exists, otherwise it defaults to a Maximum of 5 login attempts.</p><h4 id="adhoc-locking-of-user-accounts" tabindex="-1">Adhoc locking of User Accounts <a class="header-anchor" href="#adhoc-locking-of-user-accounts" aria-hidden="true">#</a></h4><p>The <code>CredentialsAuthProvider</code> also supports locking user accounts by populating the <code>UserAuth.LockedDate</code> column with a non-null value. Set it back to null to unlock the account.</p><h4 id="initializing-auth-repository-schemas" tabindex="-1">Initializing Auth Repository Schemas <a class="header-anchor" href="#initializing-auth-repository-schemas" aria-hidden="true">#</a></h4><p>Some Auth Repositories like OrmLite require an existing schema before they can be used, this can be done in the AppHost with:</p><div class="language-csharp"><pre><code><span class="token comment">//Create missing Auth Tables in any Auth Repositories that need them</span>
container<span class="token punctuation">.</span><span class="token generic-method"><span class="token function">Resolve</span><span class="token generic class-name"><span class="token punctuation">&lt;</span>IAuthRepository<span class="token punctuation">&gt;</span></span></span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">InitSchema</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> 
</code></pre></div><p>This was previously named <code>CreateMissingTables()</code> and is safe to always run as it&#39;s a NO-OP for Auth repositories that don&#39;t require a schema and only creates missing tables, so is idempotent/non-destructive on subsequent runs.</p><h4 id="new-authweb-test-project" tabindex="-1">New AuthWeb Test project <a class="header-anchor" href="#new-authweb-test-project" aria-hidden="true">#</a></h4><p>A new test project testing all Authentication providers within the same ServiceStack <a href="http://ASP.NET" target="_blank" rel="noopener noreferrer">ASP.NET</a> Web Application is in <a href="https://github.com/ServiceStack/ServiceStack/tree/master/tests/ServiceStack.AuthWeb.Tests" target="_blank" rel="noopener noreferrer">ServiceStack.AuthWeb.Tests</a>.</p><h3 id="appsettings" tabindex="-1">AppSettings <a class="header-anchor" href="#appsettings" aria-hidden="true">#</a></h3><p>AppSettings can now be passed a tier in the constructor, e.g. <code>new AppSettings(tier: &quot;Live&quot;)</code> which it uses as a prefix to reference Tier-specific appSettings first, e.g:</p><div class="language-xml"><pre><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>add</span> <span class="token attr-name">key</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>Live.AppDb<span class="token punctuation">&quot;</span></span> <span class="token attr-name">value</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>...<span class="token punctuation">&quot;</span></span> <span class="token punctuation">/&gt;</span></span>
</code></pre></div><p>Before falling back to the common key without the prefix, i.e:</p><div class="language-xml"><pre><code><span class="token tag"><span class="token tag"><span class="token punctuation">&lt;</span>add</span> <span class="token attr-name">key</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>AppDb<span class="token punctuation">&quot;</span></span> <span class="token attr-name">value</span><span class="token attr-value"><span class="token punctuation attr-equals">=</span><span class="token punctuation">&quot;</span>...<span class="token punctuation">&quot;</span></span> <span class="token punctuation">/&gt;</span></span>
</code></pre></div><p>AppSettings now allows a Parsing Strategy, e.g. You can collapse new lines when reading a complex configuration object in Web.Config <code>&lt;appSettings/&gt;</code> with:</p><div class="language-csharp"><pre><code><span class="token class-name"><span class="token keyword">var</span></span> appSettings <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token constructor-invocation class-name">AppSettings</span> <span class="token punctuation">{</span> 
    ParsingStrategy <span class="token operator">=</span> AppSettingsStrategy<span class="token punctuation">.</span>CollapseNewLines 
<span class="token punctuation">}</span><span class="token punctuation">;</span>
</code></pre></div><h3 id="nested-request-dtos" tabindex="-1">Nested Request DTOs <a class="header-anchor" href="#nested-request-dtos" aria-hidden="true">#</a></h3><p>Using <a href="https://github.com/ServiceStack/ServiceStack/commit/376ca38f604214f4d12e2f7803d8e7cfc271b725" target="_blank" rel="noopener noreferrer">nested types as Request DTO&#39;s</a> are now supported. Nested Request DTO types include the names of their containing class to form their unique name, allowing the use of multiple nested types with the same name, which is potentially interesting to be used as a versioning strategy.</p><h3 id="localized-symbols" tabindex="-1">Localized symbols <a class="header-anchor" href="#localized-symbols" aria-hidden="true">#</a></h3><p>I&#39;ve added <code>IAppHost.ResolveLocalizedString</code> support in <a href="http://bit.ly/181q0eP" target="_blank" rel="noopener noreferrer">this commit</a> which lets you override the built-in English symbols used in ServiceStack, e.g. this lets you change built-in ServiceStack routes, e.g: <code>/auth</code>, <code>/assignroles</code>, <code>?redirect=</code>, etc. into something more appropriate for your language.</p><h2 id="other-new-web-framework-features" tabindex="-1">Other New Web Framework Features <a class="header-anchor" href="#other-new-web-framework-features" aria-hidden="true">#</a></h2><ul><li>Added convenient <a href="https://github.com/ServiceStack/ServiceStack/blob/master/src/ServiceStack/ILogic.cs" target="_blank" rel="noopener noreferrer">Repository and Logic base classes</a> to reduce boilerplate when extracting logic from services into custom classes</li><li>Added <code>IAppHost.OnExceptionTypeFilter</code> to be able to customize ResponseStatus based on Exception types. Used to change the <a href="https://github.com/ServiceStack/ServiceStack/commit/17985239ed6f84b3126c651dbacd0c760a4d2951" target="_blank" rel="noopener noreferrer">default behavior of ArgumentExceptions</a> so that they&#39;re converted to field errors</li><li>Added <code>IAppHost.OnServiceException</code> so service exceptions can be intercepted and converted to different responses</li><li>Add <code>ConvertHtmlCodes</code> extension method converting HTML entities to hex-encoded entities</li><li>Add <code>Config.ScanSkipPaths</code> option to skip any plugins using the VFS to scan the filesystem (e.g. Razor/Markdown feature) from scanning specified directories, <code>/bin/</code> and <code>/obj/</code> are added by default.</li><li>Added a pre-defined <code>/swagger-ui/</code> route that loads the Swagger UI and auto configures it to look at ServiceStack services. A link to this is on the metadata page under <strong>Plugin Links</strong> heading.</li><li>Added <code>ModelFilter</code> and <code>ModelPropertyFilter</code> to allow fine-grained custom control on what&#39;s displayed in the Swagger API</li><li>Wrappers around .NET&#39;s JSON and XML DataContract Serializers now share the same <code>IStringSerializer</code> interface</li><li>Added ToMsgPack/FromMsgPack and ToProtoBuf/FromProtoBuf extension methods</li><li>Improved support for stripping App Virtual Paths when Resolving Absolute Urls useful when applications are hosted with virtual app paths as done in Amazon Web Services. This behavior can be enabled with <code>Config.StripApplicationVirtualPath = true</code>.</li><li>Support for explicitly referencing ignored DTO properties in Route PathInfo definitions, but not QueryStrings</li><li>Add support for getting Id property from runtime object type</li><li>Added support for registering a singleton instance as a runtime type</li><li>Added new <a href="https://github.com/ServiceStack/ServiceStack/commit/29d60dfa22424fe20ba35c8603686c05f88a6c25" target="_blank" rel="noopener noreferrer">IRestGateway</a> interface that typed 3rd Party gateways can use to retain a consistent and mockable interface. Specialized MockRestGateway added to stub or mock out gateways to 3rd Party services</li><li><code>__requestinfo</code> is now available on any request with <code>?debug=requestinfo</code> and is accessible to administrators or when in <strong>DebugMode</strong>, and provides in-depth diagnostics about the details of the current request and the configured AppHost including Startup errors (if any).</li><li>Plugins can register startup exceptions with <code>IAppHost.NotifyStartupException</code></li><li>Added new HTTP Headers on IHttpRequest for <code>XForwardedPort</code> and <code>XForwardedProtocol</code></li><li>Added <code>[EnsureHttps]</code> Request Filter to automatically redirect request if service was not requested under a Secure Connection with options to <strong>SkipIfDebugMode</strong> or <strong>SkipIfXForwardedFor</strong> to allow local development and requests via proxies / load-balancers in HTTP.</li><li>Users in the <strong>Admin</strong> role have super-user access giving them access to all protected resources. You can also use <code>Config.AdminAuthSecret</code> to specify a special string to give you admin access without having to login by adding <code>?authsecret=xxx</code> to the query string.</li></ul><hr><h2 id="ormlite" tabindex="-1">OrmLite <a class="header-anchor" href="#ormlite" aria-hidden="true">#</a></h2><h3 id="improved-consistency" tabindex="-1">Improved Consistency <a class="header-anchor" href="#improved-consistency" aria-hidden="true">#</a></h3><p>As the API surface of OrmLite expands it became a lot more important to focus on better consistency which now sees all alternative aliases having been removed in favor of standardized naming around SQL equivalents (e.g Select,Insert,etc). Also the parameterized APIs and the C#-like string.Format API&#39;s have now been merged with the parameterized APIs now being the default and the string.Format API having a &#39;Fmt&#39; suffix.</p><p>Most of these APIs now have XML docs and Examples for a better Intelli-sense experience. We&#39;ve also provided them in a list along side it&#39;s generated SQL in <a href="http://bit.ly/1gmrnwe" target="_blank" rel="noopener noreferrer">these API tests</a></p><p>Some notes:</p><ul><li><code>Select</code> returns a List</li><li><code>Single</code> returns 1 row (or null) if it doesn&#39;t exist</li><li><code>Scalar</code> returns a single a scalar value (e.g. int, long)</li><li><code>Where</code> is a short-hand for &#39;Select&#39; that takes a single filter</li><li><code>Count</code> is a convenience that performs an aggregate SQL Count</li><li><code>Exists</code> returns true if there were any results</li><li><code>Lazy</code> suffix indicates the results are lazily streamed</li><li><code>Column</code> returns the first column results in a List</li><li><code>ColumnDistinct</code> returns the first column unique results in a HashSet</li><li><code>Dictionary</code> returns a Dictionary made up from the first 2 columns</li><li><code>Lookup</code> returns a LINQ-like grouping in a <code>Dictionary&lt;K, List&lt;V&gt;&gt;</code></li><li><code>NonDefaults</code> suffix indicates only non-null values are used in qry</li><li><code>Only</code> suffix allows you to specify fields used on the call-site</li><li><code>Sql</code> prefix are helpers for reading and querying arbitrary raw SQL</li><li><code>Save</code> is a convenience that inserts or updates depending if it exists or not. It also now populates AutoIncrementing Id&#39;s on Inserts.</li><li>All batch operations like <code>InsertAll</code>, <code>UpdateAll</code>, <code>DeleteAll</code>, <code>SaveAll</code> participate in an existing transaction if 1 exists, otherwise a new one</li><li>Removed all &#39;where T:&#39; constraints where possible</li><li><code>OrDefault</code> APIs removed, All APIs now return null instead of throwing</li><li>autoDisposeConnection removed. false for &quot;:memory:&quot; otherwise true</li><li>Now that all OrmLite&#39;s parameterized <code>Query*</code> APIs have been merged (above), any <code>Query</code> APIs are from Dapper&#39;s extension method, which is also included in OrmLite under ServiceStack.OrmLite.Dapper namespace.</li><li>All remaining OrmLIte Attributes have been moved to ServiceStack.Interfaces, which in future will be the only dependency needed by your data models and DTOs.</li></ul><h3 id="ormlite-extension-methods-are-now-mockable" tabindex="-1">OrmLite extension methods are now mockable <a class="header-anchor" href="#ormlite-extension-methods-are-now-mockable" aria-hidden="true">#</a></h3><p>OrmLite API&#39;s can now be mocked by injecting a ResultsFilter letting you mock the results return by OrmLite which it will use instead of hitting the database. You can also mock with a filter function and it also supports nesting, see examples at: <a href="http://bit.ly/1aldecK" target="_blank" rel="noopener noreferrer">http://bit.ly/1aldecK</a></p><p>This will be useful in Unit Testing Services that access OrmLite directly instead of using a repository.</p><h3 id="support-for-references-poco-style" tabindex="-1">Support for references, POCO style <a class="header-anchor" href="#support-for-references-poco-style" aria-hidden="true">#</a></h3><p>We&#39;ve added a cool new feature to Store and Load related entities that works great on POCO which are enabled when you use the <code>[Reference]</code> attribute, e.g: <a href="http://bit.ly/1gmvtV6" target="_blank" rel="noopener noreferrer">http://bit.ly/1gmvtV6</a></p><p>Unlike normal complex properties in OrmLite:</p><ul><li>Doesn&#39;t persist as complex type blob</li><li>Doesn&#39;t impact normal querying</li><li>Saves and loads references independently from itself</li><li>Populated references get serialized in Text serializers (only populated are visible).</li><li>Data is only loaded 1-reference-level deep</li><li>Reference Fields require consistent <code>(T)Id</code> naming</li></ul><p>Basically it provides a better story when dealing with referential data that doesn&#39;t impact the POCO&#39;s ability to be used as DTO&#39;s. At the moment it&#39;s limited to loading and saving on a Single instance. We&#39;ll look at optimizations for batches on this in future.</p><p>We&#39;re going to be giving OrmLite a lot more attention from now on given that we&#39;re working full-time on ServiceStack and are using it exclusively for our .NET RDBMS persistence. We also intend on adding specialized support to take advantage of PostgreSQL&#39;s new features like their HStore and native JSON support. PostgreSQL has been offering the best features of both RDBMS and NoSQL worlds lately and has recently become a particularly attractive option now that AWS is offering first-class support for PostgreSQL in both their RDS and Redshift services.</p><hr><h2 id="servicestack-text" tabindex="-1">ServiceStack.Text <a class="header-anchor" href="#servicestack-text" aria-hidden="true">#</a></h2><ul><li>Allow adding metadata attributes to types or attributes at runtime</li><li>Add JsConfig.ExcludeTypes option to skip serialization of non-serializable properties like Streams</li><li>Change QueryString&#39;s to also adopt the configured <code>JsConfig.PropertyConvention</code> and <code>JsConfig.EmitLowercaseUnderscoreNames</code> behavior</li><li>Added an injectable ComplexTypeStrategy to the QueryStringSerializer that allows customizing the generation of complex types, e.g. can use a hash literal notation strategy with <code>QueryStringStrategy.FormUrlEncoded</code>.</li><li>Added <code>typeof(T).New()</code> extension method providing a fast way of creating new instances of static or runtime types that will use factory functions registered in the centralized <code>JsConfig.ModelFactory</code> (if configured).</li><li>The string &quot;on&quot; (i.e. the default value for HTML checkbox) is considered a <strong>true</strong> value for booleans (same with &#39;1&#39;)</li><li>The JSON serializers can be configured to support UnixTime and UnixTimeMs for DateTime&#39;s</li><li>Renamed JsonDateHandler to <code>DateHandler</code> and JsonPropertyConvention to <code>PropertyConvention</code></li></ul><h3 id="http-utils-are-now-mockable" tabindex="-1">HTTP Utils are now mockable <a class="header-anchor" href="#http-utils-are-now-mockable" aria-hidden="true">#</a></h3><p>Following in the steps of now being able to Mock OrmLite, the <a href="/http-utils.html">HTTP Utils</a> extension methods (<a href="http://bit.ly/WyV2tn" target="_blank" rel="noopener noreferrer">http://bit.ly/WyV2tn</a>) are now mockable, e.g:</p><pre><code>using (new HttpResultsFilter {
    StringResult = &quot;mocked&quot;
})
{
    //All return &quot;mocked&quot;
    &quot;http://google.com&quot;.GetJsonFromUrl();
    &quot;http://google.com&quot;.GetXmlFromUrl();
    &quot;http://google.com&quot;.GetStringFromUrl(accept: &quot;text/csv&quot;);
    &quot;http://google.com&quot;.PostJsonToUrl(json: &quot;{\\&quot;postdata\\&quot;:1}&quot;);
}
</code></pre><p>More examples showing how all HTTP Apis can be mocked are at: <a href="http://bit.ly/HdWmgm" target="_blank" rel="noopener noreferrer">http://bit.ly/HdWmgm</a></p><hr><h2 id="new-pre-release-myget-feeds" tabindex="-1">New pre-release MyGet Feeds <a class="header-anchor" href="#new-pre-release-myget-feeds" aria-hidden="true">#</a></h2><p>Instead of publishing pre-release packages on NuGet, we&#39;re instead going to release our interim packages to <a href="https://www.myget.org/" target="_blank" rel="noopener noreferrer">MyGet</a> first which provides greater control and allows better management of packages.</p><p>The Instructions to add ServiceStack&#39;s MyGet feed to <a href="http://VS.NET" target="_blank" rel="noopener noreferrer">VS.NET</a> are:</p><ol><li>Go to Tools -&gt; Options -&gt; Package Manager -&gt; Package Sources</li><li>Add the Source <strong><a href="https://www.myget.org/F/servicestack" target="_blank" rel="noopener noreferrer">https://www.myget.org/F/servicestack</a></strong> with the name of your choice, e.g. <em>ServiceStack MyGet feed</em></li></ol>__VP_STATIC_END__`,140),i=[o];function r(c,l,p,d,u,h){return a(),t("div",null,i)}var f=e(s,[["render",r]]);export{m as __pageData,f as default};
